import { Transition } from "../../components"
import { Form } from "./components/form"

export const SignUp = () => {
    return (
        <Transition>
            <Form />
        </Transition>
    )
}
